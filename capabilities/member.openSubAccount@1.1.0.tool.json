{
  "name": "member_openSubAccount",
  "description": "Opens a new sub account of a given type for a member, with an opening deposit, and returns the suffix the system assigned. The form is reached by path because MERIDIAN Core links to it from nowhere, so the goal names it. Returns suffix.",
  "input_schema": {
    "type": "object",
    "properties": {
      "memberId": {
        "type": "string",
        "description": "Institution member number",
        "pattern": "^[0-9]{5,10}$"
      },
      "accountType": {
        "type": "string",
        "description": "The kind of sub account to open",
        "enum": [
          "Savings",
          "Holiday Club",
          "Christmas Club"
        ]
      },
      "openingAmount": {
        "type": "string",
        "description": "The opening deposit, at least $25.00",
        "pattern": "^[0-9]+\\.[0-9]{2}$"
      }
    },
    "required": [
      "memberId",
      "accountType",
      "openingAmount"
    ],
    "additionalProperties": false
  }
}
