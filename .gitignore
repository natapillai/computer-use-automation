node_modules/
dist/
coverage/
.env
.env.local
!.env.example
evidence/tmp/
# Where the README's demo path writes, so following it leaves the committed
# artifacts and the committed evidence untouched.
out/
playwright-report/
test-results/
*.log
.DS_Store
Thumbs.db

# The brief is interface.ai's material and this repository is public.
# Kept on disk, never committed. See docs/CLAUDE.md section 1.
docs/ASSIGNMENT.md

# Scratch space for anything worth keeping before it is safe to commit. Spike
# scripts, spike output, transcripts. Never committed, at any point.
scratch/

# evidence/, capabilities/ and tests/fixtures/cassettes/ are committed from S3-T05 on.
# The evidence scanner in src/evidence/scanner.test.ts fails the suite if any of them
# carries a seeded canary or a redaction pattern match. See docs/EVIDENCE.md section 5.
